// SPDX-License-Identifier: MIT
// Implements a placeholder rolling hash interface.
#include "hashlab/rolling_hash.hpp"

namespace hashlab {

// Computes a rolling hash for the provided message (not yet implemented).
// Example:
//   auto result = hashlab::rolling_hash(msg);
// High level: Placeholder for a rolling hash over the input bytes.
std::uint64_t rolling_hash(bytespan msg) noexcept {
}
}
